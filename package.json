{
  "name": "gitesting",
  "version": "1.0.0",
  "description": "Gulp cli to sync branches of your choice and optionally push or pull at runtime",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "George Gomez",
  "license": "ISC",
  "dependencies": {
    "gulp": "^3.9.1"
  },
  "devDependencies": {
    "gulp-exec": "^3.0.1",
    "yargs": "^10.1.1"
  }
}
